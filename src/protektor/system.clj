(ns protektor.system)

(defn system
  "Returns a new instance of the library.
Currently, doesn't have anything to do."
  []
  {})

(defn start
  "Performs the side effects to initialize the system and start it running.
Returns an updated instance of the system."
  [system]
  {})

(defn stop
  "Performs the side effects to shut down the system and free resources.
Returns an updated instance"
  [system]
  {})
